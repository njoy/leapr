
// This certainly isn't done, but isn't used for current H in H2O example, 
// so worry about it later

auto repoz(int ntape){
  /*--------------------------------------------------------------------
   * Rewind an ENDF/B coded or blocked binary tape.
   * Positive units are coded, negative ones are blocked binary.
   *--------------------------------------------------------------------
   */
   //int i;

   if (ntape == 0) return;
   // i = std::abs(ntape);
   // rewind(i);
   return;
}



